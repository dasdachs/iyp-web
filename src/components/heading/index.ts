import { MainContainer } from "./MainContainer";
import { HeroImage } from "./HeroImage";
import { ContentSection } from "./HeroContent";

export const Heading = {
  ContentSection,
  MainContainer,
  HeroImage,
};
